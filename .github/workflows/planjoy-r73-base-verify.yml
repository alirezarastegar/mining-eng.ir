name: PlanJoy R7.3 Base Source Verify (Archived)

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Historical v2 base was incomplete; final delivery uses verified v3 source salvage.'
